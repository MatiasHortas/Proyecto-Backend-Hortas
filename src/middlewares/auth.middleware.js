export const authMiddleware = (req, res, next) => {
  // const { user } = req;
  // if (!user.email === "coderhouse@gmail.com") {
  //   next();
  // } else {
  //   res.send("Not Authorized");
  // }
};
